{
  "name": "music-downloader-backend",
  "version": "1.0.0",
  "description": "Backend para descargador de música con yt-dlp",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo 'No build needed'",
    "postinstall": "apt-get update && apt-get install -y python3 python3-pip && pip3 install yt-dlp || npm run install-ytdlp-manual",
    "install-ytdlp-manual": "curl -L https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp -o /usr/local/bin/yt-dlp && chmod a+rx /usr/local/bin/yt-dlp"
  },
  "keywords": ["youtube", "downloader", "mp3", "music", "yt-dlp"],
  "author": "Tu Nombre",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "ws": "^8.14.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
